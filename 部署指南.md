# 帕基尔项目月度结算系统 - 云服务器部署指南

## 一、购买云服务器

### 推荐方案：腾讯云轻量应用服务器

| 配置 | 规格 | 价格 | 适合 |
|------|------|------|------|
| 2核2G 3M带宽 | 入门款 | 约50元/月 | 5-10人同时使用 |
| 2核4G 5M带宽 | 推荐款 | 约70元/月 | 10-20人同时使用 |

### 购买步骤

1. 打开 https://cloud.tencent.com/product/lighthouse
2. 点击「立即购买」
3. 地域选择：选离你最近的（如广州、上海）
4. 镜像选择：**Ubuntu 22.04**（必须选这个）
5. 实例规格：2核2G 或 2核4G
6. 购买时长：建议先买1个月测试，满意后续费
7. 付款后，在控制台找到服务器的 **公网IP地址** 和 **密码**

> 阿里云也行：https://swas.console.aliyun.com （操作类似，选 Ubuntu 22.04）

---

## 二、连接到服务器

### 方法1：云控制台网页登录（最简单）

1. 在腾讯云控制台找到你的服务器
2. 点击「登录」→「标准登录方式」
3. 输入购买时设置的密码
4. 进入黑色终端窗口

### 方法2：用 SSH 工具（推荐，方便传文件）

下载 **Tabby** 或 **MobaXterm**（免费）：
- Tabby: https://tabby.sh
- MobaXterm: https://mobaxterm.mobatek.net

连接信息：
- 主机：你的服务器公网IP
- 端口：22
- 用户名：root
- 密码：购买时设置的密码

---

## 三、上传项目文件

### 方法1：用 MobaXterm（推荐，可视化拖拽）

1. 用 MobaXterm SSH 连接到服务器
2. 左侧有文件浏览器（SFTP）
3. 在服务器上创建目录：右键 → 新建文件夹 → 命名 `settlement`
4. 把本地 `settlement_system` 文件夹里的 **所有文件** 拖进去

### 方法2：用命令行（git 方式）

在服务器上执行：
```bash
# 如果项目已上传到 Git 仓库
cd /root
git clone <你的仓库地址> settlement
cd settlement
```

### 需要上传的文件

确保以下文件都在服务器上：
```
settlement/
├── app.py              ← 主程序
├── wsgi.py             ← WSGI入口
├── Dockerfile          ← Docker配置
├── docker-compose.yml  ← Docker编排
├── deploy.sh           ← 部署脚本
├── backup-cron.sh      ← 备份脚本
├── requirements.txt    ← 依赖列表
├── .dockerignore
├── templates/          ← 页面模板（12个HTML）
├── static/             ← 样式文件
├── template_files/     ← Excel模板文件（20个）
└── data/
    └── settlement.db   ← 数据库（含已有数据）
```

> **重要**：`data/settlement.db` 包含你已配置的所有部门、任务、费率数据，一定要一起上传！

---

## 四、一键部署

在服务器终端执行：

```bash
cd /root/settlement    # 进入项目目录（根据实际上传位置调整）
bash deploy.sh
```

脚本会自动完成：
1. 安装 Docker
2. 构建应用镜像
3. 启动容器
4. 显示访问地址

看到 `部署成功！` 就完成了。

---

## 五、开放端口

### 腾讯云轻量服务器

1. 进入云控制台 → 轻量应用服务器 → 找到你的服务器
2. 点击「防火墙」标签
3. 添加规则：
   - 协议：TCP
   - 端口：5000
   - 来源：0.0.0.0/0（所有IP可访问）
4. 保存

### 阿里云轻量服务器

1. 进入控制台 → 轻量应用服务器 → 服务器详情
2. 「防火墙」→「添加规则」
3. 应用类型：自定义
   - 协议：TCP
   - 端口范围：5000
4. 确认

---

## 六、访问系统

部署完成后，任何人通过浏览器访问：

```
http://你的服务器公网IP:5000
```

例如：`http://43.136.xxx.xxx:5000`

把这个链接发给各部门人员，他们浏览器打开就能用。

---

## 七、设置自动备份

在服务器上设置每天自动备份数据库：

```bash
# 编辑定时任务
crontab -e

# 在文件末尾添加这一行（每天凌晨2点备份）：
0 2 * * * /bin/bash /root/settlement/backup-cron.sh >> /root/settlement/logs/backup.log 2>&1

# 保存退出（按 Ctrl+O 保存，Ctrl+X 退出）
```

---

## 八、日常维护命令

```bash
# 进入项目目录
cd /root/settlement

# 查看运行状态
sudo docker compose ps

# 查看实时日志
sudo docker compose logs -f

# 重启服务
sudo docker compose restart

# 停止服务
sudo docker compose down

# 更新部署（上传了新文件后）
sudo docker compose down
sudo docker compose up -d --build

# 手动备份数据库
cp data/settlement.db data/backups/settlement_$(date +%Y-%m-%d_%H%M%S).db

# 查看磁盘空间
df -h
```

---

## 九、绑定域名（可选）

如果不想让别人记 IP 地址，可以买个域名：

1. 在腾讯云/阿里云买域名（约30-50元/年）
2. 添加解析：A记录 → 指向服务器IP
3. 安装 Nginx 做反向代理（需要技术操作）
4. 配置 HTTPS 证书（腾讯云有免费证书）

> 这一步需要一定技术基础，如果只是内部使用，直接用 IP:5000 就行。

---

## 十、数据迁移说明

如果服务器上已有你之前配置的数据（部门、任务、费率等），上传时确保 `data/settlement.db` 文件一起上传，这样所有配置都会保留。

如果是全新部署（没有旧数据），系统会自动初始化空数据库，需要重新在「配置管理」页面添加部门和任务。

---

## 十一、常见问题

### Q: 访问不了怎么办？
1. 检查防火墙是否开放 5000 端口
2. 检查容器是否在运行：`sudo docker compose ps`
3. 查看日志：`sudo docker compose logs`

### Q: 上传文件失败（文件太大）？
系统限制单文件最大 100MB。如需调整，修改 `app.py` 中的 `MAX_CONTENT_LENGTH`。

### Q: 服务器重启后系统还在吗？
Docker 配置了 `restart: always`，服务器重启后容器会自动启动，无需手动操作。

### Q: 怎么备份数据？
- 自动备份：已配置 crontab 每天凌晨2点
- 手动备份：`cp data/settlement.db data/backups/settlement_$(date +%Y-%m-%d).db`
- 备份文件在：`data/backups/` 目录下

### Q: 怎么升级系统？
1. 在本地修改文件
2. 重新上传到服务器
3. 执行：`sudo docker compose up -d --build`

### Q: 多人同时使用会卡吗？
2核2G 服务器支持约 10-15 人同时使用，4个工作进程处理并发。如果人多卡顿，升级到 2核4G。
